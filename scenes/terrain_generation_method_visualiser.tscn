[gd_scene format=3 uid="uid://mknok0q1uv77"]

[ext_resource type="Script" uid="uid://dxknf8qisgktm" path="res://scripts/chunk.gd" id="2_i4yfv"]
[ext_resource type="Texture2D" uid="uid://d1gpbux8tchrn" path="res://assets/water_normal_map.png" id="3_i4yfv"]
[ext_resource type="Script" uid="uid://c38b2dst17kuq" path="res://scripts/terrain_generation_method_visualiser.gd" id="3_md2q8"]

[sub_resource type="ShaderMaterial" id="ShaderMaterial_8sw0d"]

[sub_resource type="PlaneMesh" id="PlaneMesh_xfu5g"]
material = SubResource("ShaderMaterial_8sw0d")
size = Vector2(64, 64)
subdivide_width = 1022
subdivide_depth = 1022

[sub_resource type="Shader" id="Shader_i4yfv"]
code = "// NOTE: Shader automatically converted from Godot Engine 4.5.1.stable's StandardMaterial3D.

shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_back, diffuse_burley, specular_schlick_ggx;

uniform vec4 albedo : source_color;
uniform sampler2D texture_albedo : source_color, filter_linear_mipmap, repeat_enable;
uniform ivec2 albedo_texture_size;
uniform float point_size : hint_range(0.1, 128.0, 0.1);

uniform float roughness : hint_range(0.0, 1.0);
uniform sampler2D texture_metallic : hint_default_white, filter_linear_mipmap, repeat_enable;
uniform vec4 metallic_texture_channel;
uniform sampler2D texture_roughness : hint_roughness_r, filter_linear_mipmap, repeat_enable;

uniform float specular : hint_range(0.0, 1.0, 0.01);
uniform float metallic : hint_range(0.0, 1.0, 0.01);

uniform sampler2D texture_normal : hint_roughness_normal, filter_linear_mipmap, repeat_enable;
uniform float normal_scale : hint_range(-16.0, 16.0);

uniform sampler2D depth_texture : hint_depth_texture;

uniform vec3 uv1_scale;
uniform vec3 uv1_offset;
uniform vec3 uv2_scale;
uniform vec3 uv2_offset;

uniform bool circle;

void vertex() {
	UV = UV * uv1_scale.xy + uv1_offset.xy;
	VERTEX.z += sin(VERTEX.x * 0.42 + VERTEX.y * 1.152 + TIME * 0.6) * 0.2;
}

float get_alpha(vec3 world_pos) {
	if (circle) {
		return 1.0 - smoothstep(16.0 + 8.0 + 4.0 + 2.0 + 1.0 + 0.5, 32.0, sqrt(world_pos.x * world_pos.x + world_pos.z * world_pos.z));
	} else {
		return 1.0 -
			max(smoothstep(16.0 + 8.0 + 4.0 + 2.0 + 1.0 + 0.5, 32.0, world_pos.x),
			max(smoothstep(-(16.0 + 8.0 + 4.0 + 2.0 + 1.0 + 0.5), -32.0, world_pos.x),
			max(smoothstep(16.0 + 8.0 + 4.0 + 2.0 + 1.0 + 0.5, 32.0, world_pos.z),
			smoothstep(-(16.0 + 8.0 + 4.0 + 2.0 + 1.0 + 0.5), -32.0, world_pos.z))));
	}
}

void fragment() {
	vec2 base_uv = UV * 5.0 + TIME * 0.02;

	vec4 albedo_tex = texture(texture_albedo, base_uv);
	ALBEDO = albedo.rgb * albedo_tex.rgb;

	// Normal Map: Enabled
	NORMAL_MAP = texture(texture_normal, base_uv).rgb;
	NORMAL_MAP_DEPTH = normal_scale;
	
	vec3 world_pos = (INV_VIEW_MATRIX * vec4(VERTEX, 1.0)).xyz;
	ALPHA *= albedo.a * get_alpha(world_pos);
	
	float depth = texture(depth_texture, SCREEN_UV).r;
	vec3 ndc = vec3(SCREEN_UV * 2.0 - 1.0, depth);
	vec4 world = INV_VIEW_MATRIX * INV_PROJECTION_MATRIX * vec4(ndc, 1.0);
	vec3 terrain_world_pos = world.xyz / world.w;
	vec3 terrain_to_self = world_pos - terrain_world_pos;
	float dist = sqrt(terrain_to_self.x * terrain_to_self.x + terrain_to_self.z * terrain_to_self.z);
	if (dist < 0.2) ALBEDO = vec3(1.0);
	//ALBEDO += 1.0 / (dist * 25.0);
	//ALBEDO = vec3(terrain_world_position.xyz.y);
	//ALBEDO = vec3(dot(terrain_world_position.xyz, vec3(0.0, 1.0, 0.0)));
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_rec0i"]
render_priority = 1
shader = SubResource("Shader_i4yfv")
shader_parameter/albedo = Color(0.654902, 0.79607844, 1, 0.40392157)
shader_parameter/albedo_texture_size = Vector2i(0, 0)
shader_parameter/point_size = 1.0
shader_parameter/roughness = 0.0
shader_parameter/metallic_texture_channel = Vector4(1, 0, 0, 0)
shader_parameter/specular = 1.0
shader_parameter/metallic = 1.0
shader_parameter/texture_normal = ExtResource("3_i4yfv")
shader_parameter/normal_scale = 1.0
shader_parameter/uv1_scale = Vector3(1, 1, 1)
shader_parameter/uv1_offset = Vector3(0, 0, 0)
shader_parameter/uv2_scale = Vector3(1, 1, 1)
shader_parameter/uv2_offset = Vector3(0, 0, 0)
shader_parameter/circle = true

[sub_resource type="QuadMesh" id="QuadMesh_7hvas"]
material = SubResource("ShaderMaterial_rec0i")
size = Vector2(64, 64)
subdivide_width = 127
subdivide_depth = 127

[node name="TerrainGenerationMethodVisualiser" type="Node3D" unique_id=1661695000]
script = ExtResource("3_md2q8")

[node name="Planes" type="Node" parent="." unique_id=1654431943]
unique_name_in_owner = true

[node name="@MeshInstance3D@18979" type="MeshInstance3D" parent="Planes" unique_id=545511487]
extra_cull_margin = inf
mesh = SubResource("PlaneMesh_xfu5g")
script = ExtResource("2_i4yfv")

[node name="WaterMeshInstance3D" type="MeshInstance3D" parent="." unique_id=1990605936]
unique_name_in_owner = true
transform = Transform3D(1, 0, 0, 0, -4.371139e-08, 1, 0, -1, -4.371139e-08, 0, 0, 0)
visible = false
mesh = SubResource("QuadMesh_7hvas")
